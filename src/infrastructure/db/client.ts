// Database client placeholder
// TODO: Replace with proper SQLite client (better-sqlite3, libsql, or sql.js with types)
export function getDb() {
  throw new Error("Database not configured - install and configure a SQLite driver")
}

export function saveDb() {
  // No-op until database is configured
}
