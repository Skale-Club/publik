---
phase: 03-file-import
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - EDIT-05

must_haves:
  truths:
    - "User can select a DOCX file from their device"
    - "File is uploaded to server and processed by mammoth"
    - "Converted HTML content is loaded into TipTap editor"
    - "Original DOCX formatting (headings, paragraphs, lists) is preserved"
  artifacts:
    - path: "src/lib/import/docx-utils.ts"
      provides: "mammoth conversion helpers"
      exports: ["convertDocxToHtml"]
    - path: "src/app/api/import/docx/route.ts"
      provides: "Server-side DOCX import endpoint"
      exports: ["POST"]
    - path: "src/components/editor/file-import-button.tsx"
      provides: "Import UI with DOCX option"
      exports: ["FileImportButton"]
  key_links:
    - from: "src/components/editor/file-import-button.tsx"
      to: "src/app/api/import/docx/route.ts"
      via: "fetch POST with FormData"
    - from: "src/app/api/import/docx/route.ts"
      to: "src/lib/import/docx-utils.ts"
      via: "import and call convertDocxToHtml"
---

<objective>
Implement DOCX file import pipeline allowing users to import existing manuscripts from DOCX files into the TipTap editor.

Purpose: Enables users who have existing Word documents to import their content into Publik without retyping. Uses mammoth.js for reliable DOCX → HTML conversion.

Output: Working DOCX import flow with formatting preservation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-file-import/03-RESEARCH.md
@src/components/editor/tiptap-editor.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/editor/tiptap-editor.tsx:
```typescript
interface TipTapEditorProps {
  content?: string
  onChange?: (content: string) => void
  editable?: boolean
  placeholder?: string
  editor?: Editor | null
}
```
The editor instance exposes `setContent(html: string)` and `insertContent(html: string)` commands.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install mammoth and create conversion utilities</name>
  <files>src/lib/import/docx-utils.ts</files>
  <action>
    1. Run: `pnpm add mammoth`
    2. Create src/lib/import/docx-utils.ts with convertDocxToHtml function
    3. Function should accept File or ArrayBuffer, return Promise<string> of HTML
    4. Use mammoth's styleMap option to map Word headings to HTML h1/h2/h3
    5. Handle conversion warnings for debugging
  </action>
  <verify>
    <automated>pnpm exec vitest run --reporter=verbose 2>/dev/null || echo "Install mammoth and verify in package.json"</automated>
  </verify>
  <done>mammoth installed, docx-utils.ts exists with convertDocxToHtml export</done>
</task>

<task type="auto">
  <name>Task 2: Create server-side DOCX import API route</name>
  <files>src/app/api/import/docx/route.ts</files>
  <action>
    1. Create directory: src/app/api/import/docx/
    2. Create route.ts with POST handler
    3. Accept multipart/form-data with .docx file
    4. Validate file type (application/vnd.openxmlformats-officedocument.wordprocessingml.document)
    5. Call convertDocxToHtml with file buffer
    6. Return { success: true, html: string } or { error: string }
    7. Handle errors gracefully with appropriate HTTP status codes
  </action>
  <verify>
    <automated>File exists at src/app/api/import/docx/route.ts with POST export</automated>
  </verify>
  <done>API route responds to POST requests with converted HTML</done>
</task>

<task type="auto">
  <name>Task 3: Create file import UI component with DOCX option</name>
  <files>src/components/editor/file-import-button.tsx</files>
  <action>
    1. Create src/components/editor/file-import-button.tsx
    2. Component shows dropdown menu with: "Import DOCX", "Import PDF", "Import Image"
    3. Hidden file input for .docx with accept=".docx"
    4. On file select: POST to /api/import/docx with FormData
    5. On success: call onContentLoaded callback with returned HTML
    6. Add upload state (idle/uploading/error)
    7. Style with Tailwind, use lucide-react icons
  </action>
  <verify>
    <automated>File exists with FileImportButton component, hidden file input for .docx</automated>
  </done>
  <done>User can select DOCX file and content appears in editor</done>
</task>

</tasks>

<verification>
- mammoth package appears in package.json
- src/lib/import/docx-utils.ts exports convertDocxToHtml
- POST /api/import/docx accepts .docx files and returns HTML
- FileImportButton appears in editor toolbar area
- Importing a DOCX file loads content into TipTap editor with formatting
</verification>

<success_criteria>
1. User can click "Import" button and select "Import DOCX"
2. Selected DOCX file uploads to server and converts to HTML
3. Converted HTML content appears in TipTap editor
4. Headings, paragraphs, lists from DOCX are preserved as formatted content
</success_criteria>

<output>
After completion, create `.planning/phases/03-file-import/03-01-SUMMARY.md`
</output>
