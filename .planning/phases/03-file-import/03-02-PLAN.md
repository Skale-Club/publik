---
phase: 03-file-import
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - EDIT-06

must_haves:
  truths:
    - "User can select a PDF file from their device"
    - "File is uploaded to server and processed by pdfjs-dist"
    - "Extracted text is loaded into TipTap editor"
    - "Page breaks are indicated in imported content"
  artifacts:
    - path: "src/lib/import/pdf-utils.ts"
      provides: "pdfjs-dist text extraction helpers"
      exports: ["extractTextFromPdf"]
    - path: "src/app/api/import/pdf/route.ts"
      provides: "Server-side PDF import endpoint"
      exports: ["POST"]
    - path: "src/components/editor/file-import-button.tsx"
      provides: "Import UI with PDF option"
      exports: ["FileImportButton"]
  key_links:
    - from: "src/components/editor/file-import-button.tsx"
      to: "src/app/api/import/pdf/route.ts"
      via: "fetch POST with FormData"
    - from: "src/app/api/import/pdf/route.ts"
      to: "src/lib/import/pdf-utils.ts"
      via: "import and call extractTextFromPdf"
---

<objective>
Implement PDF file import pipeline allowing users to import text content from PDF files into the TipTap editor.

Purpose: Enables users who have PDF manuscripts to extract text content into Publik. Uses pdfjs-dist for text extraction. Note: PDF structure (headings, tables) is not preserved - text is imported as plain paragraphs.

Output: Working PDF import flow with text extraction.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-file-import/03-RESEARCH.md
@src/components/editor/tiptap-editor.tsx
@src/components/editor/file-import-button.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/editor/tiptap-editor.tsx:
```typescript
interface TipTapEditorProps {
  content?: string
  onChange?: (content: string) => void
  editable?: boolean
  placeholder?: string
  editor?: Editor | null
}
```
The editor instance exposes `setContent(html: string)` and `insertContent(html: string)` commands.

From 03-01-PLAN.md (shared component):
```typescript
interface FileImportButtonProps {
  onDocxSelect?: (file: File) => void
  onPdfSelect?: (file: File) => void
  onImageSelect?: (file: File) => void
  onContentLoaded?: (html: string) => void
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install pdfjs-dist and create extraction utilities</name>
  <files>src/lib/import/pdf-utils.ts</files>
  <action>
    1. Run: `pnpm add pdfjs-dist`
    2. Create src/lib/import/pdf-utils.ts with extractTextFromPdf function
    3. Function should accept File or ArrayBuffer, return Promise<string> of HTML
    4. Use pdfjs-dist to load document, iterate pages, extract text content
    5. Add <hr/> between pages to indicate page breaks
    6. Wrap each page's text in <p> tags
    7. Configure worker source for pdfjs (CDN or local)
  </action>
  <verify>
    <automated>pnpm exec vitest run --reporter=verbose 2>/dev/null || echo "Install pdfjs-dist and verify in package.json"</automated>
  </verify>
  <done>pdfjs-dist installed, pdf-utils.ts exists with extractTextFromPdf export</done>
</task>

<task type="auto">
  <name>Task 2: Create server-side PDF import API route</name>
  <files>src/app/api/import/pdf/route.ts</files>
  <action>
    1. Create directory: src/app/api/import/pdf/
    2. Create route.ts with POST handler
    3. Accept multipart/form-data with .pdf file
    4. Validate file type (application/pdf)
    5. Call extractTextFromPdf with file buffer
    6. Return { success: true, html: string } or { error: string }
    7. Handle errors gracefully with appropriate HTTP status codes
  </action>
  <verify>
    <automated>File exists at src/app/api/import/pdf/route.ts with POST export</automated>
  </done>
  <done>API route responds to POST requests with extracted text</done>
</task>

<task type="auto">
  <name>Task 3: Add PDF import to file import button component</name>
  <files>src/components/editor/file-import-button.tsx</files>
  <action>
    1. Add hidden file input for .pdf with accept=".pdf"
    2. Add "Import PDF" option to dropdown menu
    3. On file select: POST to /api/import/pdf with FormData
    4. On success: call onContentLoaded callback with returned HTML
    5. Handle and display upload errors
  </action>
  <verify>
    <automated>File has hidden file input for .pdf and handles PDF selection</automated>
  </verify>
  <done>User can select PDF file and text content appears in editor</done>
</task>

</tasks>

<verification>
- pdfjs-dist package appears in package.json
- src/lib/import/pdf-utils.ts exports extractTextFromPdf
- POST /api/import/pdf accepts .pdf files and returns extracted text
- FileImportButton handles PDF selection
- Importing a PDF file loads text content into TipTap editor
</verification>

<success_criteria>
1. User can click "Import" button and select "Import PDF"
2. Selected PDF file uploads to server and text is extracted
3. Extracted text appears in TipTap editor as paragraphs
4. Page breaks are visually indicated in imported content
</success_criteria>

<output>
After completion, create `.planning/phases/03-file-import/03-02-SUMMARY.md`
</output>
