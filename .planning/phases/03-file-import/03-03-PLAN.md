---
phase: 03-file-import
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements:
  - EDIT-07

must_haves:
  truths:
    - "User can select an image file from their device"
    - "Image is converted to base64 and inserted into TipTap editor"
    - "Supported formats include JPG, PNG, WebP"
  artifacts:
    - path: "src/lib/import/image-utils.ts"
      provides: "Image file processing helpers"
      exports: ["fileToBase64", "validateImageFile"]
    - path: "src/components/editor/file-import-button.tsx"
      provides: "Import UI with Image option"
      exports: ["FileImportButton"]
    - path: "src/components/editor/image-insert-button.tsx"
      provides: "Standalone image insert button for toolbar"
      exports: ["ImageInsertButton"]
  key_links:
    - from: "src/components/editor/file-import-button.tsx"
      to: "src/lib/import/image-utils.ts"
      via: "import and call fileToBase64"
    - from: "image-insert-button.tsx"
      to: "TipTap editor"
      via: "editor.chain().focus().setImage({ src: base64 }).run()"
---

<objective>
Implement image file import allowing users to insert images from their device directly into the TipTap editor content.

Purpose: Enables users to add images (photos, diagrams, illustrations) to their book content. Uses TipTap Image extension with base64 encoding for simplicity. Note: For production, consider uploading to storage and using URL references.

Output: Working image import flow with image insertion at cursor position.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-file-import/03-RESEARCH.md
@src/components/editor/tiptap-editor.tsx
@src/components/editor/file-import-button.tsx
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/editor/tiptap-editor.tsx:
```typescript
// TipTap Image extension already configured:
// Image.configure({ inline: false, allowBase64: true })

// Editor commands available:
// editor.chain().focus().setImage({ src: base64String }).run()
```

From 03-01-PLAN.md (shared component):
```typescript
interface FileImportButtonProps {
  onDocxSelect?: (file: File) => void
  onPdfSelect?: (file: File) => void
  onImageSelect?: (file: File) => void
  onContentLoaded?: (html: string) => void
  // For images, we need a different callback that receives base64
  onImageLoaded?: (base64: string) => void
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create image utility functions</name>
  <files>src/lib/import/image-utils.ts</files>
  <action>
    1. Create src/lib/import/image-utils.ts
    2. Create fileToBase64(file: File): Promise<string> - converts File to data URL
    3. Create validateImageFile(file: File): { valid: boolean, error?: string }
    4. Validate: file type is image/(jpeg|png|webp), max size 10MB
    5. Return base64 string with correct MIME type prefix
  </action>
  <verify>
    <automated>File exists with fileToBase64 and validateImageFile exports</automated>
  </verify>
  <done>Image utilities ready for client-side use</done>
</task>

<task type="auto">
  <name>Task 2: Update FileImportButton with image import option</name>
  <files>src/components/editor/file-import-button.tsx</files>
  <action>
    1. Add hidden file input for images with accept="image/jpeg,image/png,image/webp"
    2. Add "Import Image" option to dropdown menu
    3. Add new prop: onImageInsert?: (base64: string) => void
    4. On file select: validate with validateImageFile, convert to base64, call onImageInsert
    5. Handle validation errors with toast notification
  </action>
  <verify>
    <automated>File has hidden file input for images and handles selection</automated>
  </verify>
  <done>FileImportButton can trigger image selection</done>
</task>

<task type="auto">
  <name>Task 3: Create ImageInsertButton component for toolbar</name>
  <files>src/components/editor/image-insert-button.tsx</files>
  <action>
    1. Create src/components/editor/image-insert-button.tsx
    2. Component shows file input (styled as button with icon)
    3. On file selected: validate, convert to base64, call editor.chain().focus().setImage()
    4. Use lucide-react Image icon
    5. Handle errors gracefully
    6. This provides a direct image insert option in addition to the import menu
  </action>
  <verify>
    <automated>File exists with ImageInsertButton component that calls setImage</automated>
  </verify>
  <done>ImageInsertButton can insert images into editor</done>
</task>

</tasks>

<verification>
- src/lib/import/image-utils.ts exports fileToBase64 and validateImageFile
- FileImportButton has image option and onImageInsert callback
- ImageInsertButton component exists and integrates with TipTap
- Importing an image inserts it into editor content at cursor position
</verification>

<success_criteria>
1. User can click "Import Image" or ImageInsertButton and select an image
2. Selected image (JPG, PNG, WebP) is converted to base64
3. Image appears in TipTap editor at cursor position
4. Invalid files (wrong type, too large) show error message
</success_criteria>

<output>
After completion, create `.planning/phases/03-file-import/03-03-SUMMARY.md`
</output>
