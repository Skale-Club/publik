---
phase: 07-cover-pdf-generation
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified: []
autonomous: true
requirements:
  - COV-03
user_setup: []

must_haves:
  truths:
    - Cover PDF contains front cover, back cover, spine, and bleed (0.125" on all sides)
    - Total cover dimensions are calculated correctly for the selected trim size
    - Spine width uses calculated value from plan 01
    - Front and back covers use uploaded images from Phase 5
    - Spine text renders only when book has 80+ pages
  artifacts:
    - path: src/lib/pdf/cover-document.tsx
      provides: Full cover PDF component using @react-pdf/renderer
      exports: CoverDocument, CoverDocumentProps
    - path: src/lib/pdf/cover-dimensions.ts
      provides: KDP cover dimension calculations
      exports: calculateCoverDimensions, CoverDimensions
    - path: src/app/api/generate/cover/route.ts
      provides: Cover PDF generation API endpoint
    - path: tests/lib/pdf/cover-document.test.tsx
      provides: Unit tests for cover document
  key_links:
    - from: src/lib/pdf/cover-document.tsx
      to: src/lib/pdf/spine-calculator.ts
      via: imports calculateSpineWidth
      pattern: import.*spine-calculator
    - from: src/lib/pdf/cover-document.tsx
      to: src/domain/kdp/trim-sizes.ts
      via: imports KDP_TRIM_SIZES
      pattern: import.*trim-sizes
    - from: src/app/api/generate/cover/route.ts
      to: src/lib/pdf/cover-document.tsx
      via: imports CoverDocument
      pattern: import.*cover-document
---

<objective>
Generate a complete KDP-compliant cover PDF with front cover, back cover, spine, and bleed.

Purpose: KDP requires a single continuous cover with correct dimensions including 0.125" bleed. This plan combines the spine calculation from plan 01 with cover image data from Phase 5.

Output: Working cover PDF generation with API endpoint
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/pdf/spine-calculator.ts
@src/domain/kdp/trim-sizes.ts
@src/domain/kdp/paper-types.ts
@src/server/actions/covers.ts
@src/infrastructure/db/schema/covers.ts

<!-- Key interfaces -->
From src/lib/pdf/spine-calculator.ts:
```typescript
export type PaperType = "white" | "cream" | "premium-color" | "standard-color"
export interface SpineWidthResult {
  widthInches: number
  widthMm: number
  widthPoints: number
  canHaveSpineText: boolean
  warnings: string[]
}
export function calculateSpineWidth(pageCount: number, paperType: PaperType): SpineWidthResult
```

From src/domain/kdp/trim-sizes.ts:
```typescript
export interface TrimSize {
  id: string
  label: string
  widthIn: number
  heightIn: number
}
export const KDP_TRIM_SIZES: TrimSize[]
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cover dimension calculator</name>
  <files>src/lib/pdf/cover-dimensions.ts</files>
  <action>Create src/lib/pdf/cover-dimensions.ts with:
- Import KDP_TRIM_SIZES from @/domain/kdp/trim-sizes
- Import calculateSpineWidth from @/lib/pdf/spine-calculator
- Define CoverDimensions interface: totalWidth, totalHeight, bleedSize, spineWidth, backCoverWidth, frontCoverWidth (all in points)
- Implement calculateCoverDimensions(trimSizeId: string, pageCount: number, paperType: string) → CoverDimensions
- BLEED_INCHES = 0.125 (9 points)
- Formula: totalWidth = bleed + backCoverWidth + spineWidth + frontCoverWidth + bleed
- Formula: totalHeight = bleed + trimHeight + bleed</action>
  <verify>File exists and exports calculateCoverDimensions</verify>
  <done>Cover dimension calculator working with KDP formulas</done>
</task>

<task type="auto">
  <name>Task 2: Create cover PDF document component</name>
  <files>src/lib/pdf/cover-document.tsx</files>
  <action>Create src/lib/pdf/cover-document.tsx using @react-pdf/renderer:
- Import Document, Page, View, Image, Text, StyleSheet from @react-pdf/renderer
- Import calculateCoverDimensions from @/lib/pdf/cover-dimensions
- Define CoverDocumentProps interface with: book (has trimSizeId, paperType, pageCount, title, author), frontCoverUrl, backCoverUrl?, backCoverText?
- Create CoverDocument component that:
  - Calculates dimensions using calculateCoverDimensions
  - Sets Page size to [totalWidth, totalHeight]
  - Renders View with flexDirection: "row" containing: back section, spine section, front section
  - Back section: shows Image if backCoverUrl, otherwise Text for backCoverText
  - Spine section: shows rotated text (-90deg) only if book.pageCount >= 80
  - Front section: always shows Image from frontCoverUrl
  - Uses proper percentage-based widths for each section
- Export CoverDocument and CoverDocumentProps</action>
  <verify>File exists with valid @react-pdf/renderer component structure</verify>
  <done>Cover PDF document component created</done>
</task>

<task type="auto">
  <name>Task 3: Create cover PDF generation API endpoint</name>
  <files>src/app/api/generate/cover/route.ts</files>
  <action>Create src/app/api/generate/cover/route.ts:
- POST endpoint accepting { bookId }
- Get book data with cover images using getBookWithCovers (Phase 5 data)
- Validate: front cover image required
- Get final page count from book (or from interior PDF in Phase 6)
- Generate cover PDF using renderToBuffer(CoverDocument {...})
- Return PDF buffer with headers: Content-Type: application/pdf, Content-Disposition: attachment; filename="{title}-cover.pdf"
- Error handling: return 400 if missing front cover, 500 on generation failure</action>
  <verify>File exists with POST route returning PDF</verify>
  <done>API endpoint created for cover PDF generation</done>
</task>

</tasks>

<verification>
- [ ] Run: pnpm test --run tests/lib/pdf/cover
- [ ] Manual test: POST to /api/generate/cover with valid bookId returns PDF
- [ ] Verify PDF dimensions match KDP requirements
</verification>

<success_criteria>
- Cover PDF generates successfully with correct dimensions
- Spine width calculated correctly using plan 01 module
- Bleed of 0.125" included on all sides
- Spine text only appears for 80+ page books
</success_criteria>

<output>
After completion, create `.planning/phases/07-cover-pdf-generation/07-02-SUMMARY.md`
</output>
