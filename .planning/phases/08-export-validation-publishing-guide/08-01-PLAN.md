---
phase: 08-export-validation-publishing-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/export/FileDownloads.tsx
  - src/app/books/[bookId]/export/page.tsx
  - src/lib/export/file-download.ts
autonomous: true
requirements:
  - EXP-01
  - EXP-02

must_haves:
  truths:
    - "User can download interior PDF file from book detail page"
    - "User can download cover PDF file from book detail page"
    - "Download buttons show validation status icons"
  artifacts:
    - path: src/components/export/FileDownloads.tsx
      provides: Download buttons UI with status indicators
    - path: src/app/books/[bookId]/export/page.tsx
      provides: Export page with file download section
    - path: src/lib/export/file-download.ts
      provides: Helper functions for file download
  key_links:
    - from: src/app/books/[bookId]/export/page.tsx
      to: /api/generate/pdf
      via: fetch + blob download
    - from: src/app/books/[bookId]/export/page.tsx
      to: /api/generate/cover
      via: fetch + blob download
---

<objective>
Create individual file download UI for interior PDF and cover PDF.

Purpose: Enable users to download each file separately with validation status indicators.
Output: Export page with download buttons for interior and cover PDFs.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-export-validation-publishing-guide/08-RESEARCH.md

# Existing APIs to use
@src/app/api/generate/pdf/route.ts — Interior PDF generation endpoint
@src/app/api/generate/cover/route.ts — Cover PDF generation endpoint
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create file download helper utilities</name>
  <files>src/lib/export/file-download.ts</files>
  <action>
Create src/lib/export/file-download.ts with helper functions:
- `downloadFile(url: string, filename: string): Promise<void>` - Fetches blob and triggers download
- `getFileDownloadUrl(endpoint: string, bookId: string): string` - Constructs download URL with query params

Export types:
```typescript
export interface DownloadConfig {
  endpoint: string
  bookId: string
  filename: string
}
```
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit src/lib/export/file-download.ts</automated>
  </verify>
  <done>Helper functions created with proper TypeScript types</done>
</task>

<task type="auto">
  <name>Task 2: Create FileDownloads component</name>
  <files>src/components/export/FileDownloads.tsx</files>
  <action>
Create src/components/export/FileDownloads.tsx:
- Accepts `bookId` prop
- Renders two download buttons: "Interior PDF" and "Cover PDF"
- Each button shows:
  - Download icon (lucide-react)
  - File label
  - Validation status indicator (CheckCircle for valid, AlertTriangle for warning, XCircle for error, Loader for pending)
- Uses downloadFile helper to trigger downloads
- Shows toast notification on download success/failure (sonner)

Button states:
- Default: Gray outline button
- Hover: Slight background highlight
- Loading: Shows spinner while fetching
- Disabled: If files not yet generated

Import existing icons from lucide-react: Download, CheckCircle, AlertTriangle, XCircle, Loader2
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit src/components/export/FileDownloads.tsx</automated>
  </verify>
  <done>Component renders download buttons with icons and status indicators</done>
</task>

<task type="auto">
  <name>Task 3: Create export page with download section</name>
  <files>src/app/books/[bookId]/export/page.tsx</files>
  <action>
Create src/app/books/[bookId]/export/page.tsx:
- Server component that receives bookId from params
- Fetches book data from database to get title
- Renders a page with:
  - Page title: "Export Files - {bookTitle}"
  - Section: "Your KDP-Ready Files"
  - FileDownloads component with bookId
  - Brief description text explaining what each file is

Use Tailwind CSS for styling (page layout, spacing, typography).
Reference existing book detail pages in src/app/books/[bookId]/ for patterns.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit src/app/books/[bookId]/export/page.tsx</automated>
  </verify>
  <done>Export page created with download section accessible from book detail</done>
</task>

</tasks>

<verification>
- [ ] Navigate to /books/[bookId]/export shows download buttons
- [ ] Clicking "Interior PDF" downloads the generated PDF file
- [ ] Clicking "Cover PDF" downloads the generated cover PDF
- [ ] Page displays book title in heading
</verification>

<success_criteria>
User can navigate to /books/{bookId}/export and download both interior PDF and cover PDF files.
</success_criteria>

<output>
After completion, create `.planning/phases/08-export-validation-publishing-guide/08-01-SUMMARY.md`
</output>