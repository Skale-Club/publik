---
phase: 02-content-editor
plan: 03
type: execute
wave: 3
depends_on: ["02-01"]
files_modified:
  - src/components/editor/tiptap-editor.tsx
  - src/app/api/upload/image/route.ts
  - src/lib/storage.ts
autonomous: true
requirements:
  - EDIT-03
user_setup: []
---

<objective>
Allow users to insert images into book content. Images are uploaded and stored, then embedded in the TipTap editor.
</objective>

<context>
@src/components/editor/tiptap-editor.tsx
@src/components/editor/editor-toolbar.tsx

**TipTap Context:**
- TipTap has @tiptap/extension-image for image embedding
- Images stored as base64 or URLs
- Need upload endpoint for storing images

**Storage Context:**
- Local filesystem for development (from STACK.md)
- Abstract behind storage interface for future uploadthing migration
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install TipTap image extension</name>
  <files>package.json</files>
  <action>
    Install TipTap image extension:
    - @tiptap/extension-image
    
    Run: pnpm add @tiptap/extension-image
  </action>
  <verify>
    <automated>grep -q "@tiptap/extension-image" package.json</automated>
  </verify>
  <done>TipTap image extension installed</done>
</task>

<task type="auto">
  <name>Task 2: Create local storage utility</name>
  <files>src/lib/storage.ts</files>
  <action>
    Create storage utility for local filesystem:
    - saveImage(file: File): Promise<{url, path}>
    - deleteImage(path): Promise<void>
    - Images stored in public/uploads/books/{bookId}/
    - Returns public URL path for embedding
    
    Use Node.js fs/promises for file operations.
    Ensure directory exists before writing.
  </action>
  <verify>
    <automated>grep -q "saveImage" src/lib/storage.ts</automated>
  </done>
  <done>Storage utility created with saveImage and deleteImage functions</done>
</task>

<task type="auto">
  <name>Task 3: Create image upload API route</name>
  <files>src/app/api/upload/image/route.ts</files>
  <action>
    Create POST endpoint for image upload:
    - Accepts multipart/form-data with image file
    - Validates file type (jpg, png, gif, webp)
    - Validates file size (max 10MB)
    - Uses storage utility to save
    - Returns { url: string } on success
    - Returns error on failure
    
    Use Next.js App Router route handler.
  </action>
  <verify>
  <automated>grep -q "POST" src/app/api/upload/image/route.ts</automated>
  </verify>
  <done>Image upload endpoint created at /api/upload/image</done>
</task>

<task type="auto">
  <name>Task 4: Add image button to editor toolbar</name>
  <files>src/components/editor/editor-toolbar.tsx</files>
  <action>
    Add image upload button to EditorToolbar:
    - Button with image icon
    - Opens file picker (accept: image/*)
    - On file selected, uploads to /api/upload/image
    - On success, inserts image into editor at cursor position
    - Uses editor.chain().focus().setImage({ src: url }).run()
    - Shows loading state during upload
    - Shows error toast on failure
  </action>
  <verify>
  <automated>grep -q "extension-image" src/components/editor/editor-toolbar.tsx</automated>
  </verify>
  <done>Image button added to toolbar, uploads and inserts images correctly</done>
</task>

<task type="auto">
  <name>Task 5: Update TipTapEditor to include image extension</name>
  <files>src/components/editor/tiptap-editor.tsx</files>
  <action>
    Update TipTapEditor to include Image extension:
    - Import Image from @tiptap/extension-image
    - Add Image to editor extensions array
    - Configure with maxWidth for responsive images
    
    Add image extension configuration:
    ```typescript
    Image.configure({
      inline: false,
      allowBase64: true,
    })
    ```
  </action>
  <verify>
  <automated>grep -q "Image" src/components/editor/tiptap-editor.tsx</automated>
  </verify>
  <done>TipTapEditor updated with Image extension</done>
</task>

</tasks>

<verification>
- [ ] Image extension installed
- [ ] Storage utility saves images to filesystem
- [ ] Upload endpoint accepts and stores images
- [ ] Toolbar image button opens file picker
- [ ] Selected image uploads and inserts into editor
- [ ] Images display correctly in editor
- [ ] pnpm build exits 0
</verification>

<success_criteria>
User can click the image button in the editor toolbar, select an image file, and see it inserted into the book content. Images are stored and persist across sessions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-editor/02-03-SUMMARY.md`
</output>
