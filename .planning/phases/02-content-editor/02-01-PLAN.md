---
phase: 02-content-editor
plan: 01
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - src/components/editor/tiptap-editor.tsx
  - src/components/editor/editor-toolbar.tsx
  - src/app/(dashboard)/books/[bookId]/editor/page.tsx
  - src/app/(dashboard)/books/[bookId]/actions.ts
autonomous: true
requirements:
  - EDIT-01
user_setup: []
---

<objective>
Integrate TipTap rich text editor with book content editing. Users can write and edit book content with rich text formatting (bold, italic, headings, lists, etc.).
</objective>

<context>
@src/infrastructure/db/schema/books.ts
@src/domain/book/book.ts
@src/components/books/book-card.tsx

**Phase 1 Context:**
- Books table exists with id, title, description, trimSizeId, paperType, inkType, coverFinish
- Server Actions pattern established for CRUD
- KDP registry available in src/domain/kdp/
- Dashboard UI pattern established

**TipTap Integration Context:**
- TipTap 3.x recommended from STACK.md
- Headless editor - needs custom toolbar
- Outputs JSON/HTML for PDF generation pipeline
- Framework-agnostic with @tiptap/react adapter
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install TipTap dependencies</name>
  <files>package.json</files>
  <action>
    Install TipTap packages:
    - @tiptap/react
    - @tiptap/starter-kit
    - @tiptap/extension-placeholder
    - @tiptap/pm (ProseMirror core)
    
    Run: pnpm add @tiptap/react @tiptap/starter-kit @tiptap/extension-placeholder @tiptap/pm
  </action>
  <verify>
    <automated>grep -q "@tiptap/react" package.json</automated>
  </verify>
  <done>TipTap packages installed and listed in package.json</done>
</task>

<task type="auto">
  <name>Task 2: Create TipTap editor component</name>
  <files>src/components/editor/tiptap-editor.tsx</files>
  <action>
    Create TipTapEditor component with:
    - useEditor hook from @tiptap/react
    - StarterKit extension for basic formatting
    - Placeholder extension for empty state
    - Content prop for loading existing content
    - onChange callback for saving content
    - Basic styling with Tailwind
    - Uses "use client" directive for React hooks
    
    Editor should accept:
    - content: JSON or HTML string
    - onChange: (content: JSON) => void
    - editable: boolean (default true)
  </action>
  <verify>
    <automated>grep -q "useEditor" src/components/editor/tiptap-editor.tsx</automated>
  </verify>
  <done>TipTapEditor component created with useEditor hook, accepts content and onChange props</done>
</task>

<task type="auto">
  <name>Task 3: Create editor toolbar with formatting buttons</name>
  <files>src/components/editor/editor-toolbar.tsx</files>
  <action>
    Create EditorToolbar component with:
    - Bold, Italic, Underline buttons
    - Heading levels (H1, H2, H3)
    - Bullet list, Numbered list
    - Blockquote
    - Code block
    - Undo/Redo
    - Uses editor.chain().focus() pattern for commands
    - Active state styling for current formatting
    - Tailwind styling matching dashboard pattern
    
    Buttons should call editor.chain().focus().toggleBold().run() etc.
  </action>
  <verify>
    <automated>grep -q "toggleBold" src/components/editor/editor-toolbar.tsx</automated>
  </verify>
  <done>EditorToolbar with all formatting buttons created and integrated with editor</done>
</task>

<task type="auto">
  <name>Task 4: Create editor page route</name>
  <files>src/app/(dashboard)/books/[bookId]/editor/page.tsx</files>
  <action>
    Create editor page at /dashboard/books/[bookId]/editor:
    - Server Component that fetches book and its chapters
    - If no chapters exist, show "Create your first chapter" prompt
    - If chapters exist, render chapter selector and TipTapEditor
    - Chapter selector dropdown in page header
    - Uses sonner for notifications
    - Responsive layout with sidebar for chapter list (from Plan 02-02)
  </action>
  <verify>
    <automated>ls src/app/\(dashboard\)/books/\[bookId\]/editor/page.tsx</automated>
  </verify>
  <done>Editor page route created at /dashboard/books/[bookId]/editor</done>
</task>

</tasks>

<verification>
- [ ] TipTap packages installed (verify in package.json)
- [ ] TipTapEditor component renders without errors
- [ ] Toolbar buttons toggle formatting correctly
- [ ] Editor page loads for existing book
- [ ] pnpm build exits 0
</verification>

<success_criteria>
User can open a book and see the rich text editor with formatting toolbar. All basic formatting options (bold, italic, headings, lists) work correctly. Content changes are captured via onChange callback.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-editor/02-01-SUMMARY.md`
</output>
