---
phase: 02-content-editor
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02"]
files_modified:
  - src/components/editor/tiptap-editor.tsx
  - src/app/(dashboard)/books/[bookId]/actions.ts
autonomous: true
requirements:
  - EDIT-04
user_setup: []
---

<objective>
Implement auto-save functionality. Content auto-saves as user types with debounced server sync, persisting across browser sessions.
</objective>

<context>
@src/components/editor/tiptap-editor.tsx
@src/app/(dashboard)/books/[bookId]/actions.ts
@src/infrastructure/db/schema/chapters.ts

**Auto-save Pattern:**
- Debounce content changes (500ms-1000ms delay)
- Save to database via Server Action
- Show visual indicator (saving/saved)
- Handle offline/errors gracefully
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add updateChapterContent Server Action</name>
  <files>src/app/(dashboard)/books/[bookId]/actions.ts</files>
  <action>
    Add updateChapterContent Server Action:
    - Accepts chapterId and content (TipTap JSON)
    - Updates chapter.content in database
    - Updates chapter.updatedAt timestamp
    - Returns success/error
    - Uses revalidatePath to refresh if needed
    
    Add to existing actions file alongside chapter CRUD.
  </action>
  <verify>
  <automated>grep -q "updateChapterContent" src/app/\(dashboard\)/books/\[bookId\]/actions.ts</automated>
  </verify>
  <done>updateChapterContent Server Action created</done>
</task>

<task type="auto">
  <name>Task 2: Create useAutoSave hook</name>
  <files>src/components/editor/use-auto-save.ts</files>
  <action>
    Create useAutoSave custom hook:
    - Takes chapterId, content, and onSave callback
    - Uses useEffect with debounce (800ms delay)
    - Tracks saving state: 'idle' | 'saving' | 'saved' | 'error'
    - Calls updateChapterContent Server Action after debounce
    - Resets to 'idle' on content change
    - Handles errors gracefully with error state
    - Cleans up pending saves on unmount
    
    Debounce implementation: use setTimeout + cleanup function.
  </action>
  <verify>
  <automated>grep -q "useAutoSave" src/components/editor/use-auto-save.ts</automated>
  </done>
  <done>useAutoSave hook created with debounce logic</done>
</task>

<task type="auto">
  <name>Task 3: Integrate auto-save with TipTapEditor</name>
  <files>src/components/editor/tiptap-editor.tsx</files>
  <action>
    Update TipTapEditor to use auto-save:
    - Import useAutoSave hook
    - Pass chapterId to editor component
    - Call useAutoSave with editor.getJSON() content
    - Display save status indicator (saving/saved/error)
    
    Status indicator UI:
    - "Saving..." with spinner when saving
    - "Saved" checkmark when saved (fades after 2s)
    - "Error saving" with retry button on error
  </action>
  <verify>
  <automated>grep -q "useAutoSave" src/components/editor/tiptap-editor.tsx</automated>
  </done>
  <done>TipTapEditor integrated with auto-save, shows save status</done>
</task>

<task type="auto">
  <name>Task 4: Load chapter content on editor mount</name>
  <files>src/app/(dashboard)/books/[bookId]/editor/page.tsx</files>
  <action>
    Update editor page to load chapter content:
    - Fetch chapter data (including content JSON) from Server Action or direct DB query
    - Pass content to TipTapEditor as initial content
    - Handle case where chapter has no content yet (empty editor)
    
    This ensures content persists across browser sessions.
  </action>
  <verify>
  <automated>grep -q "chapter.content" src/app/\(dashboard\)/books/\[bookId\]/editor/page.tsx</automated>
  </done>
  <done>Editor loads existing chapter content on mount</done>
</task>

</tasks>

<verification>
- [ ] updateChapterContent Server Action works
- [ ] useAutoSave hook debounces correctly (800ms)
- [ ] Save status indicator shows during/after save
- [ ] Content persists after page refresh
- [ ] Works when typing continuously
- [ ] Handles network errors gracefully
- [ ] pnpm build exits 0
</verification>

<success_criteria>
Content auto-saves as user types. After stopping typing for ~1 second, "Saved" indicator appears. Content persists across browser sessions (refresh page and content is still there). Error handling shows appropriate feedback if save fails.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-editor/02-04-SUMMARY.md`
</output>
