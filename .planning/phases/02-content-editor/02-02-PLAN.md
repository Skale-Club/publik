---
phase: 02-content-editor
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/infrastructure/db/schema/chapters.ts
  - src/infrastructure/db/schema/index.ts
  - src/domain/book/chapter.ts
  - src/domain/book/chapter-validator.ts
  - src/app/(dashboard)/books/[bookId]/actions.ts
autonomous: true
requirements:
  - EDIT-02
user_setup: []
---

<objective>
Create chapter management system. Users can create, rename, reorder, and delete chapters within a book.
</objective>

<context>
@src/infrastructure/db/schema/books.ts
@src/domain/book/book.ts

**Phase 1 Context:**
- Books table exists with all KDP options
- Server Actions pattern established
- Dashboard UI pattern established

**Chapter Data Model:**
- Each book has multiple chapters
- Chapters need: id, bookId, title, order, content (JSON), createdAt, updatedAt
- Content stored as TipTap JSON format
- Order field for manual reordering
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create chapters database schema</name>
  <files>src/infrastructure/db/schema/chapters.ts</files>
  <action>
    Create chapters table using Drizzle sqliteTable:
    - id: text primaryKey (nanoid)
    - bookId: text foreignKey to books.id
    - title: text notNull (chapter title)
    - order: integer notNull (position in book)
    - content: text (TipTap JSON, nullable)
    - createdAt: text with datetime default
    - updatedAt: text with datetime default
    - deletedAt: text (soft delete)
    
    Add index on (bookId, order) for efficient chapter ordering.
  </action>
  <verify>
    <automated>grep -q "chapters" src/infrastructure/db/schema/index.ts</automated>
  </verify>
  <done>Chapters table schema created with all required fields</done>
</task>

<task type="auto">
  <name>Task 2: Add chapters to schema index</name>
  <files>src/infrastructure/db/schema/index.ts</files>
  <action>
    Update schema index to export chapters table:
    - Import chapters from ./chapters
    - Export chapters in the schema object
  </action>
  <verify>
    <automated>grep -q "export.*chapters" src/infrastructure/db/schema/index.ts</automated>
  </verify>
  <done>Chapters table exported from schema index</done>
</task>

<task type="auto">
  <name>Task 3: Create chapter domain types</name>
  <files>src/domain/book/chapter.ts</files>
  <action>
    Create chapter type definitions:
    - Chapter interface with all fields
    - ChapterWithBook for joined queries
    - CreateChapterInput type
    - UpdateChapterInput type
    
    Located in src/domain/book/chapter.ts
  </action>
  <verify>
    <automated>grep -q "interface Chapter" src/domain/book/chapter.ts</automated>
  </verify>
  <done>Chapter type definitions created</done>
</task>

<task type="auto">
  <name>Task 4: Create chapter Server Actions</name>
  <files>src/app/(dashboard)/books/[bookId]/actions.ts</files>
  <action>
    Add chapter CRUD Server Actions to existing actions file:
    - createChapter(bookId, title): Creates chapter with next order number
    - updateChapter(chapterId, data): Updates title/content
    - deleteChapter(chapterId): Soft delete chapter
    - reorderChapters(bookId, chapterIds): Updates order field for all chapters
    - getChapters(bookId): Returns all non-deleted chapters for a book
    
    Use Zod for validation, revalidatePath for cache invalidation.
  </action>
  <verify>
    <automated>grep -q "createChapter" src/app/\(dashboard\)/books/\[bookId\]/actions.ts</automated>
  </verify>
  <done>Chapter CRUD Server Actions implemented</done>
</task>

<task type="auto">
  <name>Task 5: Create chapter list component</name>
  <files>src/components/books/chapter-list.tsx</files>
  <action>
    Create ChapterList component:
    - Lists all chapters for a book
    - Shows chapter title and order
    - Add button to create new chapter
    - Delete button for each chapter (with confirmation)
    - Drag handle for reordering (or simple up/down buttons)
    - Click to select active chapter
    - Uses Server Actions for all mutations
    - Tailwind styling matching dashboard pattern
  </action>
  <verify>
    <automated>grep -q "ChapterList" src/components/books/chapter-list.tsx</automated>
  </verify>
  <done>ChapterList component created with create, delete, and reorder functionality</done>
</task>

</tasks>

<verification>
- [ ] Chapters table schema created and migrated
- [ ] Server Actions for chapter CRUD work
- [ ] Chapter list UI displays correctly
- [ ] Can create new chapter
- [ ] Can rename chapter
- [ ] Can delete chapter (soft delete)
- [ ] pnpm build exits 0
</verification>

<success_criteria>
User can create a new chapter, rename existing chapters, reorder chapters within a book, and delete chapters. Chapter list displays all chapters with their titles in order.
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-editor/02-02-SUMMARY.md`
</output>
