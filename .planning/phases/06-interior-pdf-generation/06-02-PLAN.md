---
phase: 06-interior-pdf-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/lib/pdf/components/page-header.tsx
  - src/lib/pdf/components/page-footer.tsx
  - src/lib/pdf/interior-document.tsx
  - src/lib/pdf/layout-options.ts
autonomous: true
requirements:
  - PDF-02
  - PDF-03

must_haves:
  truths:
    - "Every page displays page number in format 'Page X of Y'"
    - "Headers can optionally display book title and/or chapter name"
    - "Footers display page numbers that remain fixed across page breaks"
  artifacts:
    - path: "src/lib/pdf/components/page-header.tsx"
      provides: "Configurable header component for PDF"
      exports: ["PDFHeader"]
    - path: "src/lib/pdf/components/page-footer.tsx"
      provides: "Footer with page numbers and optional text"
      exports: ["PDFFooter"]
    - path: "src/lib/pdf/layout-options.ts"
      provides: "Layout configuration options for headers/footers"
      exports: ["LayoutOptions", "defaultLayoutOptions"]
  key_links:
    - from: "src/lib/pdf/components/page-footer.tsx"
      to: "@react-pdf/renderer"
      via: "render prop for pageNumber, totalPages"
    - from: "src/lib/pdf/interior-document.tsx"
      to: "src/lib/pdf/components/page-header.tsx"
      via: "PDFHeader component usage"
---

<objective>
Implement page numbers, headers, and footers for interior PDF generation.

Purpose: Professional PDF output with consistent page numbering and optional header text
Output: PDF with page numbers on every page, configurable headers/footers
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/pdf/toc-document.tsx — Existing footer with page number pattern (lines 127-133)
@src/lib/pdf/interior-document.tsx — Created in Plan 06-01

<interfaces>
From src/lib/pdf/toc-document.tsx (existing pattern):
```typescript
// Footer with page number - pattern to reuse
<Text
  style={documentStyles.footer}
  render={({ pageNumber, totalPages }) =>
    `Page ${pageNumber} of ${totalPages}`
  }
  fixed
/>
```

Key insight: The `fixed` prop ensures the footer appears on every page without repeating in the document flow. The `render` prop receives pageNumber and totalPages on second pass.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LayoutOptions configuration</name>
  <files>src/lib/pdf/layout-options.ts</files>
  <action>
    Create src/lib/pdf/layout-options.ts with:
    - Interface LayoutOptions for header/footer configuration
    - Options: showHeader (boolean), headerText (string | null), headerBookTitle (boolean), headerChapterName (boolean), showFooter (boolean), footerText (string | null), showPageNumbers (boolean), pageNumberFormat ("Page X of Y" | "X of Y" | "Page X" | "X")
    - Default options object with sensible defaults
    
    This allows users to configure headers/footers in book settings.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit src/lib/pdf/layout-options.ts</automated>
  </verify>
  <done>LayoutOptions interface exported with default values</done>
</task>

<task type="auto">
  <name>Task 2: Create PDFHeader component</name>
  <files>src/lib/pdf/components/page-header.tsx</files>
  <action>
    Create src/lib/pdf/components/page-header.tsx:
    - Accept props: bookTitle, chapterTitle?, layout: LayoutOptions
    - Display book title if headerBookTitle is true
    - Display chapter title if headerChapterName is true and provided
    - Display custom headerText if provided
    - Use fixed prop to render on every page
    - Style with subtle font (10pt, gray color #666)
    
    Create the components directory if it doesn't exist.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit src/lib/pdf/components/page-header.tsx</automated>
  </verify>
  <done>PDFHeader component renders configured header on every page</done>
</task>

<task type="auto">
  <name>Task 3: Create PDFFooter component with page numbers</name>
  <files>src/lib/pdf/components/page-footer.tsx</files>
  <action>
    Create src/lib/pdf/components/page-footer.tsx:
    - Accept props: layout: LayoutOptions
    - Show page numbers based on pageNumberFormat setting
    - Use render prop to get pageNumber and totalPages
    - Show custom footerText if provided
    - Use fixed prop for every-page rendering
    - Handle two-pass rendering: show "..." on first pass when totalPages is undefined
    
    Follow existing pattern from toc-document.tsx lines 127-133.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit src/lib/pdf/components/page-footer.tsx</automated>
  </verify>
  <done>PDFFooter component shows page numbers in configured format</done>
</task>

<task type="auto">
  <name>Task 4: Integrate headers/footers into InteriorDocument</name>
  <files>src/lib/pdf/interior-document.tsx</files>
  <action>
    Update src/lib/pdf/interior-document.tsx:
    - Import PDFHeader and PDFFooter components
    - Add layoutOptions prop to BookSettings
    - Add Header component inside each Page (before content)
    - Add Footer component inside each Page (after content)
    - Pass current chapter title to header for chapter-specific headers
    
    Update BookSettings interface to include layoutOptions.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit src/lib/pdf/interior-document.tsx</automated>
  </verify>
  <done>InteriorDocument renders headers and footers on all content pages</done>
</task>

</tasks>

<verification>
- [ ] Generated PDF shows "Page X of Y" on every page
- [ ] Generated PDF shows book title in header when configured
- [ ] Generated PDF shows chapter names in header when configured
- [ ] First render pass shows "..." (totalPages undefined), second pass shows correct numbers
</verification>

<success_criteria>
Every page in generated PDF displays page number in configured format. Optional headers show book title and/or chapter name.
</success_criteria>

<output>
After completion, create `.planning/phases/06-interior-pdf-generation/06-02-SUMMARY.md`
</output>