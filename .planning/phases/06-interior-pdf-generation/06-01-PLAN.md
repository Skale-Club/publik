---
phase: 06-interior-pdf-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/pdf/interior-document.tsx
  - src/lib/pdf/page-layout.ts
  - src/lib/pdf/components/chapter-content.tsx
  - src/app/api/generate/pdf/route.ts
  - src/lib/pdf/font-registration.ts
autonomous: true
requirements:
  - PDF-01
  - PDF-05

must_haves:
  truths:
    - "PDF page dimensions exactly match the selected KDP trim size"
    - "All 16 KDP trim sizes are supported (5x8 through 8.5x11 and A4)"
    - "Trim size is selected from book settings and passed to PDF generation"
  artifacts:
    - path: "src/lib/pdf/interior-document.tsx"
      provides: "Main PDF document with KDP trim size mapping"
      exports: ["InteriorDocument"]
    - path: "src/lib/pdf/page-layout.ts"
      provides: "Trim size to PDF page dimension conversion"
      exports: ["getPageDimensions", "getTrimSizeLabel"]
    - path: "src/app/api/generate/pdf/route.ts"
      provides: "Updated PDF generation endpoint with KDP trim sizes"
      updates: "mapTrimSize function"
  key_links:
    - from: "src/lib/pdf/page-layout.ts"
      to: "src/domain/kdp/trim-sizes.ts"
      via: "KDP_TRIM_SIZES import"
    - from: "src/app/api/generate/pdf/route.ts"
      to: "src/lib/pdf/page-layout.ts"
      via: "getPageDimensions call"
---

<objective>
Implement interior PDF generation with support for all 16 KDP trim sizes, replacing the current A4/LETTER-only mapping.

Purpose: Enable users to generate PDFs with exact KDP trim dimensions (e.g., 6x9, 5.5x8.5, 8x10)
Output: Interior PDF with correct page dimensions matching user's selected trim size
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/domain/kdp/trim-sizes.ts — KDP_TRIM_SIZES array with all 16 sizes
@src/lib/pdf/toc-document.tsx — Existing PDF document structure to reference
@src/app/api/generate/pdf/route.ts — Current PDF generation endpoint

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/domain/kdp/trim-sizes.ts:
```typescript
export interface TrimSize {
  id: string
  label: string
  widthIn: number
  heightIn: number
  widthCm: number
  heightCm: number
  isLarge: boolean
  maxPages: Record<PaperInkCombo, number>
}

export const KDP_TRIM_SIZES: TrimSize[] = [
  { id: "5x8", label: '5" × 8"', widthIn: 5, heightIn: 8, ... },
  { id: "6x9", label: '6" × 9"', widthIn: 6, heightIn: 9, ... },
  // ... all 16 sizes
]
```

From src/lib/pdf/toc-document.tsx:
```typescript
export interface BookSettings {
  title: string
  author: string
  trimSize?: "A4" | "LETTER"
  // ... needs update to support KDP trim IDs
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create page-layout.ts with KDP trim size mapping</name>
  <files>src/lib/pdf/page-layout.ts</files>
  <action>
    Create src/lib/pdf/page-layout.ts with functions:
    - getPageDimensions(trimSizeId: string): { width: number; height: number } — converts KDP trim size to points (1 inch = 72 points)
    - getTrimSizeLabel(trimSizeId: string): string — returns display label (e.g., '6" × 9"')
    - isValidTrimSize(trimSizeId: string): boolean — validates trim size exists
    
    Use KDP_TRIM_SIZES from @/domain/kdp. Return custom object { width, height } for @react-pdf/renderer (not string "A4"/"LETTER").
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit src/lib/pdf/page-layout.ts</automated>
  </verify>
  <done>src/lib/pdf/page-layout.ts exports getPageDimensions, getTrimSizeLabel, isValidTrimSize</done>
</task>

<task type="auto">
  <name>Task 2: Create interior-document.tsx with KDP trim sizes</name>
  <files>src/lib/pdf/interior-document.tsx</files>
  <action>
    Create src/lib/pdf/interior-document.tsx as the main PDF document component:
    - Import BookSettings from existing toc-document.tsx and extend to accept trimSizeId: string (not "A4"/"LETTER")
    - Use getPageDimensions() from page-layout.ts to set Page size prop
    - Render TOC page followed by chapter content pages
    - Include page numbers using render prop pattern (existing in toc-document.tsx)
    
    Reference existing toc-document.tsx for structure but use KDP trim sizes instead of A4/LETTER.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit src/lib/pdf/interior-document.tsx</automated>
  </verify>
  <done>InteriorDocument accepts trimSizeId and renders PDF with correct dimensions</done>
</task>

<task type="auto">
  <name>Task 3: Update PDF generation API to use KDP trim sizes</name>
  <files>src/app/api/generate/pdf/route.ts</files>
  <action>
    Update src/app/api/generate/pdf/route.ts:
    - Replace mapTrimSize() with import from page-layout.ts
    - Pass trimSizeId from book database to InteriorDocument
    - Fetch paper_type_id from book to calculate page count for margins later
    - Keep existing chapter fetching logic
    
    Use existing API structure but replace trim size mapping with KDP-compliant version.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit src/app/api/generate/pdf/route.ts</automated>
  </verify>
  <done>API route generates PDF with exact KDP trim dimensions matching book.trim_size_id</done>
</task>

</tasks>

<verification>
- [ ] Request PDF for book with trim size "6x9" returns PDF with page dimensions 432x648 points
- [ ] Request PDF for book with trim size "5x8" returns PDF with page dimensions 360x576 points
- [ ] Request PDF for book with trim size "8.5x11" returns PDF with page dimensions 612x792 points
- [ ] All 16 KDP trim sizes produce correct dimensions
</verification>

<success_criteria>
PDF generation endpoint accepts any of the 16 KDP trim sizes and produces PDF with exact matching dimensions.
</success_criteria>

<output>
After completion, create `.planning/phases/06-interior-pdf-generation/06-01-SUMMARY.md`
</output>