---
phase: 06-interior-pdf-generation
plan: 05
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-04
files_modified:
  - src/lib/pdf/trim-size-validator.ts
  - src/lib/pdf/page-layout.ts
  - tests/lib/pdf/trim-sizes.test.ts
autonomous: true
requirements:
  - PDF-05

must_haves:
  truths:
    - "All 16 KDP trim sizes produce PDF with exact matching dimensions"
    - "PDF dimensions verified to match KDP specification in inches"
    - "Trim size validation confirms dimensions are accurate before PDF generation"
  artifacts:
    - path: "src/lib/pdf/trim-size-validator.ts"
      provides: "Validation that PDF dimensions match selected trim size"
      exports: ["validateTrimSize", "getExpectedDimensions"]
    - path: "tests/lib/pdf/trim-sizes.test.ts"
      provides: "Test suite for all 16 KDP trim sizes"
  key_links:
    - from: "src/lib/pdf/trim-size-validator.ts"
      to: "src/domain/kdp/trim-sizes.ts"
      via: "KDP_TRIM_SIZES import"
    - from: "tests/lib/pdf/trim-sizes.test.ts"
      to: "src/lib/pdf/page-layout.ts"
      via: "getPageDimensions calls"
---

<objective>
Validate that all 16 KDP trim sizes produce PDF with exact matching dimensions.

Purpose: Ensure dimension accuracy across all KDP trim sizes (5x8 through 8.5x11 and A4)
Output: Test suite and validator confirming dimension accuracy
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/domain/kdp/trim-sizes.ts — KDP_TRIM_SIZES with all 16 sizes and their dimensions
@src/lib/pdf/page-layout.ts — Created in Plan 01

All 16 KDP trim sizes:
- 5x8, 5.06x7.81, 5.25x8, 5.5x8.5, 6x9, 6.14x9.21, 6.69x9.61, 7x10
- 7.44x9.69, 7.5x9.25, 8x10, 8.25x6, 8.25x8.25, 8.5x8.5, 8.5x11, 8.27x11.69 (A4)

Dimensions are in inches in KDP_TRIM_SIZES. Must convert to points (x 72) for @react-pdf/renderer.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create trim-size-validator.ts</name>
  <files>src/lib/pdf/trim-size-validator.ts</files>
  <action>
    Create src/lib/pdf/trim-size-validator.ts:
    - Import { KDP_TRIM_SIZES } from @/domain/kdp
    - Import { getPageDimensions } from ./page-layout
    - Create getExpectedDimensions(trimSizeId): { width: number, height: number, widthIn: number, heightIn: number }
      - Returns dimensions in both points and inches
    - Create validateTrimSize(trimSizeId): { valid: boolean, expected: {...}, actual: {...}, match: boolean }
      - Compares getPageDimensions output with KDP_TRIM_SIZES specification
    - Export list of all valid trim size IDs for dropdown UI
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit src/lib/pdf/trim-size-validator.ts</automated>
  </verify>
  <done>Trim size validator exports validation function and expected dimensions</done>
</task>

<task type="auto">
  <name>Task 2: Create test suite for all 16 KDP trim sizes</name>
<files>tests/lib/pdf/trim-sizes.test.ts</files>
<action>
    Create tests/lib/pdf/trim-sizes.test.ts:
    - Import { KDP_TRIM_SIZES } from @/domain/kdp
    - Import { getPageDimensions, getTrimSizeLabel } from @/lib/pdf/page-layout
    - Test that getPageDimensions returns correct dimensions for each of 16 trim sizes
    - Test that dimensions in points = dimensions in inches * 72
    - Test that getTrimSizeLabel returns correct display labels
    - Test that invalid trim size ID returns default or throws clear error
    
    Create tests directory if it doesn't exist.
  </action>
  <verify>
    <automated>pnpm test --run tests/lib/pdf/trim-sizes.test.ts</automated>
  </verify>
  <done>Test suite verifies all 16 KDP trim sizes produce correct dimensions</done>
</task>

<task type="auto">
  <name>Task 3: Add dimension validation to PDF API (optional/enhancement)</name>
  <files>src/app/api/generate/pdf/route.ts</files>
<action>
    Optional: Add validation to PDF generation route:
    - Before generating PDF, validate trim size exists in KDP_TRIM_SIZES
    - Log warning if trim size not found, use default
    - This ensures robust handling of any trim size value in database
    
    This is optional - core functionality is covered by tests.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit src/app/api/generate/pdf/route.ts</automated>
  </verify>
  <done>API validates trim size before PDF generation</done>
</task>

</tasks>

<verification>
- [ ] Test passes for 5x8: 360x576 points (5x8 * 72)
- [ ] Test passes for 6x9: 432x648 points (6x9 * 72)
- [ ] Test passes for 8.5x11: 612x792 points (8.5x11 * 72)
- [ ] All 16 KDP trim sizes have passing tests

Manual verification (outside test suite):
- Generate PDF for each trim size
- Verify page dimensions match KDP specification exactly
</verification>

<success_criteria>
All 16 KDP trim sizes produce PDF with exact matching dimensions. Tests verify dimensions match specification.
</success_criteria>

<output>
After completion, create `.planning/phases/06-interior-pdf-generation/06-05-SUMMARY.md`
</output>