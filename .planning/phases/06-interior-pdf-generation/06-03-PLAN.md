---
phase: 06-interior-pdf-generation
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/lib/pdf/font-registration.ts
  - src/lib/pdf/interior-document.tsx
  - public/fonts/README.md
autonomous: true
requirements:
  - PDF-04

must_haves:
  truths:
    - "All fonts in PDF are fully embedded (not subset) for KDP compliance"
    - "PDF uses bundled TTF fonts instead of system fonts"
    - "pdffonts tool verifies fonts are embedded, not Type 3 or missing"
  artifacts:
    - path: "src/lib/pdf/font-registration.ts"
      provides: "Font registration for bundled TTF fonts"
      exports: ["registerKDPFonts", "getFontFamily"]
    - path: "src/lib/pdf/interior-document.tsx"
      provides: "PDF using registered fonts instead of system fonts"
      updates: "fontFamily references"
    - path: "public/fonts/"
      provides: "Directory containing TTF font files for embedding"
  key_links:
    - from: "src/lib/pdf/font-registration.ts"
      to: "@react-pdf/renderer"
      via: "Font.register API"
    - from: "src/lib/pdf/interior-document.tsx"
      to: "src/lib/pdf/font-registration.ts"
      via: "registerKDPFonts() call"
---

<objective>
Implement font embedding with bundled TTF fonts for KDP compliance.

Purpose: KDP requires fully embedded fonts, not system font references
Output: PDF with all fonts embedded using bundled TTF files
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/lib/pdf/toc-document.tsx — Existing font usage (Helvetica)
@src/lib/pdf/interior-document.tsx — Created in Plan 06-01

<interfaces>
From @react-pdf/renderer documentation:
```typescript
import { Font } from "@react-pdf/renderer"

// Register font family
Font.register({
  family: "Times-Roman",
  src: "https://unpkg.com/@react-pdf/core@4.3.2/fonts/fonts/Times-Roman.ttf",
})

// Use in styles
const styles = StyleSheet.create({
  text: {
    fontFamily: "Times-Roman",
  }
})
```

KDP-approved fonts to register:
- Times-Roman, Times-Italic, Times-Bold, Times-BoldItalic
- Helvetica, Helvetica-Oblique, Helvetica-Bold, Helvetica-BoldOblique
- Courier (optional, for code blocks)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create font-registration.ts module</name>
  <files>src/lib/pdf/font-registration.ts</files>
  <action>
    Create src/lib/pdf/font-registration.ts:
    - Import Font from @react-pdf/renderer
    - Create registerKDPFonts() function that registers the following font families:
      - Times-Roman, Times-Italic, Times-Bold, Times-BoldItalic (from bundled TTF or CDN)
      - Helvetica, Helvetica-Oblique, Helvetica-Bold, Helvetica-BoldOblique
      - Courier (for code blocks)
    - Use CDN URLs from unpkg (as shown in research) as fallback, or point to local files in /public/fonts/
    - Export getFontFamily(fontName: string) helper that returns standardized font family name
    - Call registerKDPFonts() at module load (once) for automatic registration
    
    Ensure fonts are registered only once (check Font.getRegistered()).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit src/lib/pdf/font-registration.ts</automated>
  </verify>
  <done>Font registration module exports registerKDPFonts and getFontFamily</done>
</task>

<task type="auto">
  <name>Task 2: Update InteriorDocument to use registered fonts</name>
  <files>src/lib/pdf/interior-document.tsx</files>
  <action>
    Update src/lib/pdf/interior-document.tsx:
    - Import { registerKDPFonts } from font-registration.ts (import at top to trigger registration)
    - Replace all fontFamily: "Helvetica" with fontFamily: "Helvetica" (will work after registration)
    - Use Times-Roman for body text, Times-Bold for chapter titles
    - Use Courier for any code blocks
    
    Ensure the import triggers Font.register() call on module load.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit src/lib/pdf/interior-document.tsx</automated>
  </verify>
  <done>InteriorDocument uses registered KDP fonts instead of system fonts</done>
</task>

<task type="auto">
<parameter name>Task 3: Create fonts directory and README</name>
<files>public/fonts/README.md</files>
  <action>
    Create public/fonts/ directory (if using local fonts) and public/fonts/README.md:
    - Document which fonts should be placed in this directory
    - Explain that fonts are bundled for embedding
    - Note: For v1, use CDN URLs as source to avoid large bundle, but document where local TTF files would go
    
    If using CDN (unpkg), document this as the source of truth.
  </action>
  <verify>
    <automated>ls public/fonts/</automated>
  </verify>
  <done>public/fonts/README.md documents font sources</done>
</task>

</tasks>

<verification>
- [ ] Generated PDF uses Times/Helvetica fonts (not system fonts)
- [ ] pdffonts command shows "Type: Type 1" or "TrueType" with "embedded" status
- [ ] No fallback to Helvetica (system) font references

Manual verification command (after plan execution):
```bash
pdffonts output.pdf | grep -E "Times|Helvetica|Courier"
```
Expected: font shows as embedded, not "not embedded"
</verification>

<success_criteria>
All text in generated PDF uses fully embedded fonts. pdffonts verifies fonts are embedded, not subsetted or missing.
</success_criteria>

<output>
After completion, create `.planning/phases/06-interior-pdf-generation/06-03-SUMMARY.md`
</output>