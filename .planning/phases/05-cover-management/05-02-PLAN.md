---
phase: 05-cover-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/infrastructure/db/schema/covers.ts
  - src/lib/covers/index.ts
  - src/components/covers/BackCoverInput.tsx
  - src/server/actions/covers.ts
autonomous: true
requirements:
  - COV-02
user_setup: []

must_haves:
  truths:
    - User can choose between image or text mode for back cover
    - Back cover image is stored with proper metadata
    - Back cover text is stored and can be retrieved
    - Switching between modes preserves existing data appropriately
  artifacts:
    - path: src/infrastructure/db/schema/covers.ts
      provides: Back cover data storage
      contains: backCoverType, backCoverImageUrl, backCoverImageWidth, backCoverImageHeight, backCoverText
    - path: src/components/covers/BackCoverInput.tsx
      provides: Back cover input UI
      features: radio toggle for image/text, textarea for text, file input for image
    - path: src/server/actions/covers.ts
      provides: Server actions for cover CRUD
      actions: saveBackCover, getCover
  key_links:
    - from: BackCoverInput.tsx
      to: covers.ts
      via: saveBackCover server action
      pattern: saveBackCover.*formData
    - from: BackCoverInput.tsx
      to: /api/upload/cover
      via: fetch POST for image upload
      pattern: fetch.*api/upload/cover.*back
---

<objective>
Implement back cover input supporting both image upload and text content modes.

Purpose: Allow users to either upload a pre-designed back cover image or enter text content that will be rendered on the back cover in Phase 7.

Output: Extended covers schema with back cover fields, BackCoverInput component, server actions for saving.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cover-management/05-01-SUMMARY.md
@src/infrastructure/db/schema/covers.ts
@src/components/covers/CoverUploader.tsx (from 05-01)

# Key Context from 05-01
- Covers table exists with front cover fields
- Cover upload endpoint exists at /api/upload/cover
- Validation utilities exist in src/lib/covers/

# Back Cover Schema Fields (to add)
backCoverType: 'image' | 'text'
backCoverImageUrl?: string
backCoverImageWidth?: number  
backCoverImageHeight?: number
backCoverText?: string
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend covers schema with back cover fields</name>
  <files>src/infrastructure/db/schema/covers.ts</files>
  <action>
Update src/infrastructure/db/schema/covers.ts to add back cover fields:
- backCoverType: text with enum ['image', 'text'] - mode selection
- backCoverImageUrl: text - uploaded image URL
- backCoverImageWidth: integer - for validation
- backCoverImageHeight: integer - for validation  
- backCoverText: text - back cover text content

Keep existing front cover fields. Ensure fields are optional since user may not have added back cover yet.
  </action>
  <verify>
<automated>tsc --noEmit (schema compiles)</automated>
  </verify>
  <done>Covers table updated with back cover dual-mode fields</done>
</task>

<task type="auto">
  <name>Task 2: Create cover server actions</name>
  <files>src/server/actions/covers.ts</files>
  <action>
Create src/server/actions/covers.ts:
- getCover(bookId): Fetch cover record by bookId, returns cover data or null
- saveFrontCover(bookId, url, width, height): Upsert front cover data
- saveBackCover(bookId, data): Upsert back cover data
  - data: { type: 'image' | 'text', imageUrl?: string, imageWidth?: number, imageHeight?: number, text?: string }
- deleteCover(bookId): Remove cover record

Use Drizzle ORM. Use upsert pattern (INSERT ... ON CONFLICT UPDATE) for save operations.
Export all actions for use in components.
  </action>
  <verify>
<automated>tsc --noEmit (actions compile)</automated>
  </done>
  <done>Cover CRUD server actions available</done>
</task>

<task type="auto">
  <name>Task 3: Create BackCoverInput component</name>
  <files>src/components/covers/BackCoverInput.tsx</files>
  <action>
Create src/components/covers/BackCoverInput.tsx:
- Props: bookId, initialType?, initialImageUrl?, initialText?, onSave(data)
- State: coverType ('image' | 'text'), text content, uploading state
- UI:
  - Radio button group to toggle between "Text Back Cover" and "Image Back Cover"
  - If text mode: textarea for back cover description (author bio, book blurb, etc.)
  - If image mode: file input accepting image/jpeg, image/png, image/tiff, image/webp
  - Save button that calls saveBackCover server action
- Handle mode switching: when switching to image, keep text in state but don't show; when switching to text, keep image URL but don't show
- Show preview of uploaded image
- Display appropriate help text for each mode

Reference TipTap editor styling if available, or use standard Tailwind forms.
  </action>
  <verify>
<automated>tsc --noEmit (component compiles)</automated>
  </verify>
  <done>BackCoverInput component supports dual mode with proper UI</done>
</task>

</tasks>

<verification>
- [ ] Covers schema has backCoverType, backCoverImageUrl, backCoverText fields
- [ ] Server actions can save and retrieve back cover data
- [ ] BackCoverInput shows radio toggle between image/text modes
- [ ] Text mode shows textarea for entering back cover content
- [ ] Image mode allows uploading back cover image
- [ ] Switching modes preserves existing data appropriately
</verification>

<success_criteria>
User can choose between uploading a back cover image or entering text content. Both modes save correctly to the database and can be retrieved.

Next: Plan 05-03 adds KDP validation integration to ensure covers meet specifications.
</success_criteria>

<output>
After completion, create `.planning/phases/05-cover-management/05-02-SUMMARY.md`
</output>
