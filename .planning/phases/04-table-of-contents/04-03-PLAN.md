---
phase: 04-table-of-contents
plan: "03"
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - src/lib/pdf/toc-document.tsx
  - src/components/pdf/toc-page.tsx
  - src/lib/toc/transform.ts
  - src/app/api/generate/pdf/route.ts
autonomous: true
requirements:
  - TOC-03

must_haves:
  truths:
    - Generated PDF includes a visual table of contents page
    - PDF bookmarks appear in PDF reader sidebar navigation
    - TOC links navigate to correct pages
    - Bookmark hierarchy matches TOC entry levels
  artifacts:
    - path: src/lib/pdf/toc-document.tsx
      provides: PDF document component with TOC page and bookmarks
    - path: src/lib/toc/transform.ts
      provides: Transform TOC entries to PDF structure format
    - path: src/components/pdf/toc-page.tsx
      provides: Visual TOC page component for PDF
  key_links:
    - from: src/server/actions/toc.ts
      to: src/lib/toc/transform.ts
      via: transformTOCForPDF function converts DB entries to PDF structure
    - from: src/lib/pdf/toc-document.tsx
      to: @react-pdf/renderer
      via: Document component with bookmark prop
---

<objective>
Integrate TOC with PDF generation pipeline - include visual TOC page and PDF bookmarks.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-table-of-contents/04-RESEARCH.md
@src/types/toc.ts
@src/server/actions/toc.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TOC transform utilities</name>
  <files>src/lib/toc/transform.ts</files>
  <action>
    Create utilities to transform TOC entries for PDF generation:
    - transformTOCForPDF(entries): Convert TOCEntry[] to PDF TOC structure
      - Extract id, title, level, anchorId
      - Generate page reference placeholder (will be resolved during render)
    - PDFTOCEntry: { id, title, level, anchorId, pageNumber? }
    - generateBookmark(entries): Create bookmark array for @react-pdf/renderer
      - Format: { title: string, fit: true, expanded?: boolean }
  </action>
  <verify>
  <automated>grep -n "export.*transformTOCForPDF" src/lib/toc/transform.ts</automated>
  </done>
  <done>Transform utilities convert TOC entries to PDF-ready format</done>
</task>

<task type="auto">
<parameter name>Task 2: Create visual TOC page component</name>
  <files>src/components/pdf/toc-page.tsx</files>
  <action>
    Create React PDF component for visual TOC page:
    - Props: entries (PDFTOCEntry[])
    - Layout: Title "Table of Contents", then list entries
    - Entry display: Title with indentation by level, dot leaders, page number
    - Use @react-pdf/renderer: Text, View, Link, StyleSheet
    - Level indentation: level * 15pt left margin
    - Dot leaders: String repetition between title and page number
    - Link: href="#{anchorId}" for internal navigation
    Reference research Pattern 3 for @react-pdf/renderer syntax.
  </action>
  <verify>
  <automated>grep -l "Table of Contents" src/components/pdf/toc-page.tsx</automated>
  </done>
  <done>Visual TOC page component ready for PDF rendering</done>
</task>

<task type="auto">
  <name>Task 3: Create PDF TOC document component</name>
  <files>src/lib/pdf/toc-document.tsx</files>
  <action>
    Create main PDF document component with TOC integration:
    - Props: book, chapters, tocEntries
    - Structure:
      1. TOC Page (first page, bookmark="Table of Contents")
      2. Content pages with chapter headings (each has bookmark prop)
    - For each heading/chapter in content:
      - Add bookmark={{ title: entry.title, fit: true, expanded: entry.level === 1 }}
      - Add id={entry.anchorId} to Text component for internal linking
    - Use render prop for page numbers: {({ pageNumber }) => ...}
    - Handle the two-pass render: show "..." on first pass, real page numbers on second
  </action>
  <verify>
  <automated>grep -n "bookmark=" src/lib/pdf/toc-document.tsx</automated>
  </done>
  <done>PDF document with bookmarks and TOC page integrated</done>
</task>

<task type="auto">
  <name>Task 4: Update PDF generation endpoint</name>
  <files>src/app/api/generate/pdf/route.ts</files>
  <action>
    Update or create PDF generation API to include TOC:
    - Fetch TOC entries for the book: getTOCEntries(bookId)
    - Transform entries: transformTOCForPDF(entries)
    - Use TOCDocument component instead of basic Document
    - Pass transformed entries to document component
    - Ensure book settings (trim size, margins) are passed for proper page formatting
  </action>
  <verify>
  <automated>grep -l "tocEntries" src/app/api/generate/pdf/route.ts</automated>
  </done>
  <done>PDF generation endpoint includes TOC in output</done>
</task>

</tasks>

<verification>
- [ ] Build passes: pnpm build exits 0
- [ ] PDF generation produces file with TOC page
- [ ] PDF bookmarks appear in reader sidebar
- [ ] Clicking TOC entry navigates to correct page
</verification>

<success_criteria>
- Visual TOC page appears as first page of generated PDF
- PDF bookmarks appear in reader navigation sidebar
- Bookmark hierarchy matches TOC entry levels
- Internal links in TOC navigate to correct pages
- Page numbers display correctly (handles two-pass render)
</success_criteria>

<output>
After completion, create `.planning/phases/04-table-of-contents/04-03-SUMMARY.md`
</output>
