---
phase: 04-table-of-contents
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/toc/headings.ts
  - src/lib/toc/index.ts
  - src/types/toc.ts
autonomous: true
requirements:
  - TOC-01

must_haves:
  truths:
    - System automatically extracts headings from TipTap editor content
    - TOC updates in real-time when headings change in the editor
    - Heading IDs are deterministic (slugified) for consistent linking
  artifacts:
    - path: src/types/toc.ts
      provides: TypeScript interfaces for TOC entries and anchors
    - path: src/lib/toc/headings.ts
      provides: Heading extraction utilities from TipTap editor
    - path: src/lib/toc/index.ts
      provides: TOC library exports and configuration
  key_links:
    - from: src/components/editor/tiptap-editor.tsx
      to: src/lib/toc/headings.ts
      via: import and use in editor extension config
      pattern: TableOfContents.*getId.*slugify
---

<objective>
Implement automatic TOC generation from TipTap editor headings using the official TableOfContents extension.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-table-of-contents/04-RESEARCH.md
@src/components/editor/tiptap-editor.tsx
@src/infrastructure/db/schema/chapters.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install TOC extension and dependencies</name>
  <files>package.json</files>
  <action>
    Install the following packages:
    - @tiptap/extension-table-of-contents (3.20.5)
    - slugify for generating deterministic IDs
    Run: pnpm add @tiptap/extension-table-of-contents @tiptap/pm
    Run: pnpm add slugify
  </action>
  <verify>
    <automated>pnpm list @tiptap/extension-table-of-contents slugify</automated>
  </verify>
  <done>Packages installed and visible in package.json</done>
</task>

<task type="auto">
  <name>Task 2: Create TOC type definitions</name>
  <files>src/types/toc.ts</files>
  <action>
    Create TypeScript interfaces for TOC functionality:
    - TOCEntry: id, bookId, title, level, anchorId, position, isCustom, createdAt, updatedAt
    - Anchor: id, textContent, level, originalLevel, pos, isActive, isScrolledOver
    - TOCSyncResult: added, updated, removed entries
    Export all types from this file.
  </action>
  <verify>
    <automated>grep -l "export interface TOCEntry" src/types/toc.ts</automated>
  </verify>
  <done>Types file exists with TOCEntry, Anchor, and TOCSyncResult interfaces exported</done>
</task>

<task type="auto">
  <name>Task 3: Create heading extraction utilities</name>
  <files>src/lib/toc/headings.ts</files>
  <action>
    Create utility functions for extracting headings from TipTap:
    - slugify(text): Generate deterministic ID from heading text (lowercase, replace spaces with hyphens, remove special chars)
    - getHierarchicalIndexes(): Return hierarchical index array (1, 1.1, 1.1.1) per TipTap docs
    - extractAnchorsFromEditor(editor): Extract current anchors from editor.storage.tableOfContents.anchors
    - subscribeToTOCUpdates(editor, callback): Set up onUpdate listener for real-time TOC changes

    Reference the TipTap TOC extension documentation pattern from research.
  </action>
  <verify>
    <automated>grep -n "export function slugify" src/lib/toc/headings.ts</automated>
  </verify>
  <done>Heading extraction utilities with slugify, getHierarchicalIndexes, and subscription functions</done>
</task>

<task type="auto">
  <name>Task 4: Create TOC library index</name>
  <files>src/lib/toc/index.ts</files>
  <action>
    Create the main TOC library entry point:
    - Export all utilities from headings.ts
    - Export types from @/types/toc
    - Create default configuration object for TableOfContents extension with:
      - anchorTypes: ['heading']
      - getIndex: getHierarchicalIndexes
      - getId: (content) => slugify(content)
      - onUpdate: (callback will be handled in component)
  </action>
  <verify>
    <automated>grep -n "TableOfContents.configure" src/lib/toc/index.ts</automated>
  </verify>
  <done>TOC library exports and extension configuration ready</done>
</task>

</tasks>

<verification>
- [ ] Build passes: pnpm build exits 0
- [ ] TOC extension integrates with existing TipTap editor
- [ ] Headings with IDs are visible in editor storage when headings exist
</verification>

<success_criteria>
- TipTap TableOfContents extension installed and configured
- slugify function generates consistent IDs for headings
- TOC anchors update in real-time when editor content changes
- Type definitions available for downstream components
</success_criteria>

<output>
After completion, create `.planning/phases/04-table-of-contents/04-01-SUMMARY.md`
</output>
