---
phase: 04-table-of-contents
plan: "02"
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/infrastructure/db/schema/toc.ts
  - src/lib/toc/sync.ts
  - src/server/actions/toc.ts
  - src/components/editor/toc-sidebar.tsx
  - src/components/editor/toc-entry.tsx
autonomous: true
requirements:
  - TOC-02

must_haves:
  truths:
    - User can view a list of TOC entries extracted from headings
    - User can rename TOC entries (changes are stored separately from headings)
    - User can reorder TOC entries via drag-and-drop
    - User can add custom TOC entries not linked to headings
    - User can remove TOC entries
    - Custom entries persist across editor sessions
  artifacts:
    - path: src/infrastructure/db/schema/toc.ts
      provides: Database schema for storing editable TOC entries
    - path: src/server/actions/toc.ts
      provides: Server actions for TOC CRUD operations
    - path: src/components/editor/toc-sidebar.tsx
      provides: Sidebar component displaying TOC with editing controls
    - path: src/components/editor/toc-entry.tsx
      provides: Individual TOC entry row with edit controls
  key_links:
    - from: src/components/editor/tiptap-editor.tsx
      to: src/components/editor/toc-sidebar.tsx
      via: Editor page layout renders sidebar alongside editor
    - from: src/components/editor/toc-sidebar.tsx
      to: src/server/actions/toc.ts
      via: Server action calls for CRUD operations
---

<objective>
Build editable TOC UI with database persistence, allowing users to rename, reorder, add, and remove TOC entries.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-table-of-contents/04-RESEARCH.md
@src/infrastructure/db/schema/chapters.ts
@src/types/toc.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TOC database schema</name>
  <files>src/infrastructure/db/schema/toc.ts</files>
  <action>
    Create Drizzle schema for TOC entries:
    - Table name: toc_entries
    - Columns: id (UUID primary key), bookId (FK to books), title (text), level (integer), anchorId (text, nullable), position (integer), isCustom (boolean), createdAt (timestamp), updatedAt (timestamp)
    - Index on bookId for fast queries
    - Reference: src/infrastructure/db/schema/chapters.ts for table patterns
  </action>
  <verify>
    <automated>grep -l "toc_entries" src/infrastructure/db/schema/toc.ts</automated>
  </verify>
  <done>TOC entries table schema created with all required fields</done>
</task>

<task type="auto">
  <name>Task 2: Install drag-and-drop dependencies</name>
  <files>package.json</files>
  <action>
    Install @dnd-kit packages for sortable TOC list:
    - @dnd-kit/core
    - @dnd-kit/sortable
    - @dnd-kit/utilities
    Run: pnpm add @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
  </action>
  <verify>
    <automated>pnpm list @dnd-kit/core</automated>
  </verify>
  <done>Drag-and-drop packages installed</done>
</task>

<task type="auto">
  <name>Task 3: Create TOC sync utilities</name>
  <files>src/lib/toc/sync.ts</files>
  <action>
    Create sync logic to keep stored TOC entries in sync with editor headings:
    - syncTOCWithHeadings(bookId, anchors): Compare anchors with stored entries
      - New headings: create new TOC entries
      - Removed headings: mark as removed (keep for undo)
      - Changed headings: preserve custom title, update anchor reference
    - getTOCEntriesForBook(bookId): Fetch all TOC entries ordered by position
    - isCustomEntry(entry): Returns true if entry.isCustom or title differs from anchor text
    - debounceSync(): Utility to debounce sync calls to avoid excessive DB writes
  </action>
  <verify>
    <automated>grep -n "export.*syncTOCWithHeadings" src/lib/toc/sync.ts</automated>
  </done>
  <done>Sync utilities handle bi-directional sync between headings and stored entries</done>
</task>

<task type="auto">
  <name>Task 4: Create TOC server actions</name>
  <files>src/server/actions/toc.ts</files>
  <action>
    Create server actions for TOC CRUD:
    - getTOCEntries(bookId): Fetch all entries for a book
    - updateTOCEntry(id, title): Update entry title
    - reorderTOCEntries(bookId, entryIds): Update position based on array order
    - addTOCEntry(bookId, entry): Add custom entry
    - removeTOCEntry(id): Remove entry
    - syncTOC(bookId, anchors): Run full sync with headings
    Use Zod for input validation. Reference src/domain/book/chapter-validator.ts for validation patterns.
  </action>
  <verify>
  <automated>grep -n "export.*getTOCEntries" src/server/actions/toc.ts</automated>
  </verify>
  <done>Server actions for all TOC CRUD operations implemented</done>
</task>

<task type="auto">
  <name>Task 5: Create TOC Entry component</name>
  <files>src/components/editor/toc-entry.tsx</files>
  <action>
    Create individual TOC entry row component:
    - Props: entry (TOCEntryType), isLinkedToHeading (boolean), onUpdate, onRemove
    - Display: Level indicator (indent based on level), Title (editable input), Delete button
    - Show visual indicator for custom vs auto-generated entries (icon or badge)
    - Inline edit mode: click title to edit, blur/enter to save
    - Use useState for edit mode toggle
  </action>
  <verify>
  <automated>grep -l "TOCEntry" src/components/editor/toc-entry.tsx</automated>
  </verify>
  <done>Reusable TOC entry component with inline editing and delete</done>
</task>

<task type="auto">
  <name>Task 6: Create TOC Sidebar component</name>
  <files>src/components/editor/toc-sidebar.tsx</files>
  <action>
    Create TOC sidebar component:
    - Props: bookId, editor (TipTap instance), entries (from DB), onUpdateEntry, onReorder, onRemove, onAdd
    - Subscribe to editor TOC changes via onUpdate callback
    - Display entries list with drag handles using @dnd-kit
    - "Add Custom Entry" button at bottom
    - Real-time sync: when editor headings change, trigger syncTOC
    - Debounce DB updates to avoid excessive writes
    - Style: Fixed sidebar, scrollable entry list, collapsible
  </action>
  <verify>
  <automated>grep -l "DndContext" src/components/editor/toc-sidebar.tsx</automated>
  </done>
  <done>Full TOC sidebar with drag-and-drop reordering, inline editing, and add/remove</done>
</task>

</tasks>

<verification>
- [ ] Build passes: pnpm build exits 0
- [ ] TOC sidebar renders alongside editor
- [ ] Drag-and-drop reordering works
- [ ] Custom entries can be added and removed
- [ ] Entry titles can be edited inline
</verification>

<success_criteria>
- TOC entries stored in database and persist across sessions
- Users can rename, reorder, add, and remove entries
- Custom entries are visually distinguished from auto-generated ones
- TOC syncs automatically when editor headings change
- Drag-and-drop reordering works with @dnd-kit
</success_criteria>

<output>
After completion, create `.planning/phases/04-table-of-contents/04-02-SUMMARY.md`
</output>
